name: REUSE Compliance Check

on:
  workflow_call:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v7
    - name: REUSE Compliance Check
      uses: fsfe/reuse-action@v6
